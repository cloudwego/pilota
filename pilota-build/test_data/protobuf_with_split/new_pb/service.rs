pub mod service {
    #![allow(warnings, clippy::all)]
    use ::pilota::{Buf as _, BufMut as _, pb::descriptor_getter::*};
    pub mod service {
        use ::pilota::{Buf as _, BufMut as _, pb::descriptor_getter::*};
        include!("service/mod.rs");
    }
}
