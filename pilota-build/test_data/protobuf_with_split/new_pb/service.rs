pub mod service {
    #![allow(warnings, clippy::all)]
    use ::pilota::{Buf as _, BufMut as _};
    pub mod service {
        use ::pilota::{Buf as _, BufMut as _};
        include!("service/mod.rs");
    }
}
